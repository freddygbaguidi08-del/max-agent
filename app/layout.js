export const metadata = {
  title: "MAX Agent",
  description: "Agent IA intelligent",
};

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}
